/**
 * Compatibility export: Customer is now Partner (partners collection).
 * Prefer importing Partner for new code. Facades filter with isCustomer.
 */
import Partner from './Partner.js';

export default Partner;

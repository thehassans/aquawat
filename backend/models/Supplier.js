/**
 * Compatibility export: Supplier is now Partner (partners collection).
 * Prefer importing Partner for new code. Facades filter with isVendor.
 */
import Partner from './Partner.js';

export default Partner;
